# Blueprint Agent Instructions

This folder is an app-owned Blueprint sidecar. Work sidecar-first and preserve the authority of each layer:

- Start with the Blueprint MCP `validate` tool using `{ "project": "<this-folder>" }`, then use `index` to discover stable boundary IDs. Use focused `query` calls and deep `extract` packets before reading the canvas DOM.
- Structured JSON owns stable IDs, declarations, named states, dependency relationships, production relationships, targets, review conditions, and saved exploration/history metadata. Keep `manifest.json`, `tokens.json`, `primitives.json`, `components.json`, and `screens.json` internally consistent; keep each saved comparison in `explorations/<id>.json` and each prior canonical version in `history/<screen>-v<n>.json`.
- Governed primitive HTML/CSS is the canonical reusable implementation. The Primitives board, components, and screens must instantiate that same source. Governed component HTML/CSS owns reusable assemblies. Screen HTML/CSS owns unique semantic hierarchy, responsive layout, media, effects, and art direction.
- Compose reusable boundaries with `<blueprint-use kind="primitive|component" ref="<stable-id>" state="<named-state>">`. Declare every rendered reusable dependency in JSON; do not duplicate a recurring control in screen-local markup or add an app-specific Blueprint runtime branch. Blueprint replaces the authoring element with the referenced app root and annotates that root directly, so write layout selectors against semantic component/primitive roots rather than `<blueprint-use>`.
- Use token CSS custom properties for reusable colors, type, spacing, shape, shadow, and motion. Record deliberate screen-local literals in `prototype.localValueExceptions` with the exact CSS property and literal value; strict validation rejects undeclared color literals and lengths that restate a same-role token. Instantiate control primitives with `<blueprint-use>` rather than hand-building native buttons, inputs, or selects; mark a deliberate native control with `data-blueprint-native="<reason>"`.
- Keep fonts, images, and media inside `manifest.prototypeHost.assetRoots`, list screen assets in `prototype.assetRefs`, and keep sources self-contained. Do not add remote assets, undeclared filesystem refs, arbitrary JavaScript, or nondeterministic runtime state. Review links must remain safe local destinations and compile to inert metadata; forms must not declare actions or submitting controls in the static no-network slice.
- A new sidecar starts with exactly two empty base frames: `screen:home` on `phone` and `screen:web-home` on `desktop-web`. Map real app tokens and recurring controls, then populate those frames with governed screen sources before adding more routes or states. Do not invent product copy to fill an empty canvas.
- Keep frame naming structured: `productionRelationship.routePath` owns the route and `name` owns the short plain-language frame name. Blueprint derives the visible `Route · Frame name` label. If multiple screen records intentionally share both values, give every one a unique consecutive positive integer `version` starting at `1`; Blueprint then appends `· Vn`. Omit `version` for a single frame, and do not put routes, machine IDs, states, viewports, or version markers inside `name`.
- Use the MCP `explore` tool for two to five alternatives to one exact screen state and viewport. Edit only the returned candidate HTML/CSS sources, use exploration queries and `serve` with `explorationId` for review, and do not add candidates to `screens.json`. Leave unresolved explorations active or archive them; never delete their saved baseline or candidate sources. Call `promote` only after the person explicitly chooses a candidate and supply all current digests returned by inspection so stale work cannot overwrite a newer canonical screen silently. Use history queries plus `restore` when the person chooses a prior version; restoration must preserve the outgoing current screen as the next immutable history entry.
- The 22 Blueprint base primitives in `primitives.json` are universal and locked: keep the full base set with its named state sets and states. Base primitives take on this app's design through token values; add app primitives, state sets, and states as needed, but never remove or reduce the base set — validation rejects reductions for high-fidelity sidecars.
- Run the `validate` tool in `baseline` mode after structural edits. For implementation-bound work, also call it in `readiness` and `strict` modes; readiness reports evidence and fidelity tiers, and does not replace strict validation.
- Review with the `serve` MCP tool using `{ "project": "<this-folder>" }`. When the person refers to "this", "that", or the selected element, call the `selection` tool to read what they selected in the canvas before editing. Capture a declared condition with `capture` using a `screen:<id>` boundary plus its named `state`, `viewport`, and PNG `out` path.
- Screenshots, diffs, review manifests, and canvas inspection are evidence of what was reviewed, not editable source or dependency truth. Keep generated evidence under ignored `.blueprint-artifacts/` and do not commit it by default.
- Legacy sidecars without canonical sources remain `baseline-compatible`. Their generic fallback output must never be described as high-fidelity proof.
- Keep this folder single-project. Blueprint is a prototype and handoff surface, not a production service runtime, dashboard, registry, code generator, or cross-app project manager.
